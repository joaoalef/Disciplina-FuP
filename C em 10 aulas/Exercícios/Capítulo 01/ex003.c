#include <stdio.h>

// Faça um programa em C que imprima a média aritmética entre os números 5, 8, 12.

int main(){
    int media;
    media = (5, 8, 12) / 3;

    printf("%d", media);

    return 0;
}