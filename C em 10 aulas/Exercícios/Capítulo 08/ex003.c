#include <stdio.h>

/*
Considere um vetor vet de tamanho 20.
Construa um programa com algoritmos recursivos para calcular:

• o elemento máximo do vetor;
• o elemento mínimo do vetor;
• a soma dos elementos do vetor;
• o produto dos elementos do vetor;
• a média dos elementos do vetor.
*/

int maximo(int vet[20]);
int minimo(int vet[20]);
int soma(int vet[20]);
int produto(int vet[20]);
int media(int vet[20]);


void main(){
    int vet[20];

    printf("Digite 20 termos para o vetor:\n");
    for(int i = 0; i < 20; i++)
        scanf("%d", &vet[i]);
}

// Retorna o máximo valor do vetor.
int maximo(int vet[20]){

}

// Retorna o mínimo valor do vetor.
int minimo(int vet[20]){

}

// Retorna a soma dos valores do vetor.
int soma(int vet[20]){

}

// Retorna o produto dos valores do vetor.
int produto(int vet[20]){

}

// Retorna a media dos valores do vetor.
int media(int vet[20]){

}